## Arrays in Python

* Arrays have fixed capacity under the hood
* Arrays resize automatically
* Access O(1)
* Append O(1) - **Amortized** ( ocassionally it resizes, but average cost over n appends is O(1))