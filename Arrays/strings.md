## Strings in python

* Immutable - cannot be modified in place. You can create a copy to modify
* iterate and build string:
```
parts = []
for ch in ['a','b','c']:
    parts.append(ch)
return "".join(parts)

```





