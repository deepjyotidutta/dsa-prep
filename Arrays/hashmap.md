## Hashmap or Dict & Sets in Python

* O(1) get,set,delete
* f = defaultdict(int) # sets default 0 values
* Use Dict when need to store associated data

* s = set()
* set operation : & (intersection) , | (union) , - (diff)
* Use set when ONLY need to trace existance e.g. Have I see this number before ?